{
  "name": "amqp-printer-client",
  "version": "0.0.1",
  "description": "Connects to AMQP, listens to queue, prints web pages when message recieved or raw text or base64 images",
  "main": "lib/index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JP Silvashy",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.5.1",
    "colors": "^1.1.2",
    "js-yaml": "^3.8.3",
    "node-uuid": "^1.4.8",
    "printer": "^0.2.2"
  }
}
